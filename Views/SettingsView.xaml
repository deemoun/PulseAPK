<UserControl x:Class="PulseAPK.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:properties="clr-namespace:PulseAPK.Properties"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="{x:Static properties:Resources.SettingsHeader}" Foreground="{StaticResource Brush.TextPrimary}" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

            <TextBlock Text="{x:Static properties:Resources.ApktoolPath}" Foreground="{StaticResource Brush.TextSecondary}" Margin="0,0,0,5"/>
            <DockPanel>
                <TextBox Text="{Binding ApktoolPath, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource CyberTextBoxStyle}" Width="400"/>
                <Button Content="{x:Static properties:Resources.Browse}" Command="{Binding BrowseApktoolCommand}" Style="{StaticResource CyberButtonStyle}" Margin="10,0,0,0" Padding="16,0" MinWidth="90"/>
            </DockPanel>
            <TextBlock Text="{Binding JavaPathDisplay}" Foreground="{StaticResource Brush.TextSecondary}" Margin="0,5,0,0" TextWrapping="Wrap"/>
            <StackPanel Margin="0,5,0,0">
                <StackPanel Orientation="Horizontal" Margin="0,0,0,3">
                    <Button Content="Download ApkTool"
                            Style="{StaticResource CyberButtonStyle}"
                            CommandParameter="https://bitbucket.org/iBotPeaches/apktool/downloads/"
                            Click="LinkButton_Click"
                            Padding="12,4"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <Button Content="Download JAVA"
                            Style="{StaticResource CyberButtonStyle}"
                            CommandParameter="https://www.azul.com/downloads/?package=jdk#zulu"
                            Click="LinkButton_Click"
                            Padding="12,4"/>
                </StackPanel>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
